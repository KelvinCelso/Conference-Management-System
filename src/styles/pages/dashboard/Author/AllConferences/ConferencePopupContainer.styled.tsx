import styled from "styled-components";

export const StyledConferencePopupContainer = styled.div`
  position: fixed;
  inset: 0;
  background-color: transparent;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
`;
